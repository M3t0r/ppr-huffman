#include "cmdargs.h"
#include <string.h>

cmdargs parse_args(int argc, char **argv)
{
    cmdargs a;
    /* gibt einige felder die man initialisieren müsste, hier der rundum schlag für alles */
    memset(&a, 0, sizeof(cmdargs));
    
    return a;
}
